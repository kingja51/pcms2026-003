-- ============================================================================
-- ROLL_EDITOR 역할 삭제 (2026-07-31)
-- ----------------------------------------------------------------------------
-- V2026073101 이 001 실측대로 시드한 7종 중 `ROLL_EDITOR` 하나를 제거한다.
-- 사용하지 않기로 확정했다.
--
-- ※ V2026073101 을 고치지 않는다 — 이미 적용된 마이그레이션을 수정하면
--   체크섬 불일치로 기동이 실패한다(개발가이드 §6-5, 상시 게이트 8).
--   그래서 새 버전을 추가한다.
--
-- 남기는 6종은 "권한만 만들어 둔" 상태다:
--   ROLE_SUPER · ROLE_ADMIN · ROLE_STAFF · ROLE_MEMBER
--   ROLL_MANAGER · ROLL_PRIVACY   ← ROLE_ 오타는 001 실측 그대로 유지(PLAN §7 기록)
--
-- 삭제 전 참조 실측(2026-07-31): 아래 4곳 모두 0건이라 물리 삭제가 안전하다.
--   tb_role_url_access.required_roles / tb_admin_role / tb_role_auth / tb_role_hierarchy
--   tb_role 을 참조하는 FK 5개가 있으므로, 참조가 생긴 뒤라면 delete_yn='Y' 로 바꿔야 한다.
-- ============================================================================

DELETE FROM `tb_role`
 WHERE `role_id` = '00000000-0000-7000-8000-000000000013'
   AND `role_code` = 'ROLL_EDITOR';
