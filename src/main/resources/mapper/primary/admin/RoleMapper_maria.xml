<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gonet.primary.admin.mapper.RoleMapper">

  <select id="findActiveOptions" resultType="com.gonet.primary.admin.dto.RoleOption">
    SELECT role_id, role_code, role_name, site_id
      FROM tb_role
     WHERE delete_yn = 'N' AND use_yn = 'Y'
     ORDER BY COALESCE(site_id,''), sort_order ASC, role_code ASC
  </select>

    <delete id="deleteOldDeleted">
        DELETE FROM tb_role
        WHERE delete_yn = 'Y'
        AND updated_at &lt;= DATE_SUB(NOW(), INTERVAL 1 MONTH)
    </delete>

</mapper>
