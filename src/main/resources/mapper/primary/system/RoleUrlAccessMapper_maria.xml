<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  tb_role_url_access 조회 Mapper.
  컬럼→프로퍼티 매핑: mybatis.configuration.map-underscore-to-camel-case=true.
-->
<mapper namespace="com.gonet.primary.system.access.mapper.RoleUrlAccessMapper">

  <select id="findAllActive"
          resultType="com.gonet.primary.system.access.dto.RoleUrlAccessRule">
    SELECT
      url_access_id, site_id, url_pattern, http_method, access_type,
      required_roles, required_auths, allowed_user_types, allowed_ips,
      require_csrf_yn, require_2fa_yn, priority
    FROM tb_role_url_access
    WHERE use_yn    = 'Y'
      AND delete_yn = 'N'
    ORDER BY priority ASC, url_pattern ASC
  </select>
</mapper>
