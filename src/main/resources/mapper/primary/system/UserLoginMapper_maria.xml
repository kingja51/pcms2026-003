<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  v_user_login VIEW 조회 Mapper.
  컬럼→프로퍼티 매핑: mybatis.configuration.map-underscore-to-camel-case=true
-->
<mapper namespace="com.gonet.primary.system.login.mapper.UserLoginMapper">

  <sql id="loginCols">
    user_type, user_id, uniq_id, site_id, group_id, department_id, department_name,
    login_id, password, status, login_fail_count, locked_until,
    last_login_at, password_changed_at, password_expire_at,
    two_factor_enabled_yn, two_factor_secret,
    ip_whitelist, allowed_time_from, allowed_time_to,
    role_ids, role_codes, group_ids, delete_yn
  </sql>

  <select id="findByLoginId"
          resultType="com.gonet.primary.system.login.dto.UserLogin">
    SELECT <include refid="loginCols"/>
      FROM v_user_login
     WHERE login_id  = #{loginId}
       AND delete_yn = 'N'
     ORDER BY FIELD(user_type, 'ADMIN', 'EMPLOYEE', 'MEMBER')
     LIMIT 1
  </select>

  <select id="findByLoginIdInTypes"
          resultType="com.gonet.primary.system.login.dto.UserLogin">
    SELECT <include refid="loginCols"/>
      FROM v_user_login
     WHERE login_id  = #{loginId}
       AND delete_yn = 'N'
       AND user_type IN
       <foreach collection="userTypes" item="t" open="(" separator="," close=")">
         #{t}
       </foreach>
     ORDER BY FIELD(user_type, 'ADMIN', 'EMPLOYEE', 'MEMBER')
     LIMIT 1
  </select>
</mapper>
